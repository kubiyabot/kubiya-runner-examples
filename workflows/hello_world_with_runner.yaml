version: 3
type: conversation
steps:
  - id: response
    type: action
    action:
      name: simple_action
      store: tester
      parameters: {}
  - id: show_response
    type: message
    prompt: "${response}"
